# Sesión 3 — mayo 2026

## Decisiones editoriales
- **Catálogo de Capa AR/VR reducido a 6 opciones**. Sacamos Cosm Domes y Meta Horizon. Agrupación por accesibilidad: "desde tu casa hoy" / "con equipo dedicado". ADR 28, enmienda al ADR 15.
- **BTS histórico en Camino al Mundial**. Sección "el camino en video" al pie del módulo road, con videos de YouTube oficiales curados por selección. Piloto con Argentina. ADR 29.

## Decisiones técnicas
- **Cover images locales para Capa AR/VR** (next/image, 16:9, public/immersive/). Sprint 4d-3d.
- **BTS hardcodeado + thumbnail/link** (no API, no embed, no LLM en runtime). Sprint 4d-3e. ADR 29.
- **Modelo del road: Sonnet → Haiku** para bajar costo y latencia. ADR 30.
- **Cleanup pre-4d-3e**: fix de hydration del mapa Ojo de Dios (Math.random fuera del useState inicial); endpoint road devuelve 200 con error en body en vez de 500 (consistencia con immersive); aprendizaje de find/replace anclado agregado a docs/CLAUDE.md.

## Sprints deployados
- **4d-3d** (ADR 28): agrupación por accesibilidad + covers en capa inmersiva.
- **4d-3e** (ADR 29 + 30): "el camino en video" (piloto Argentina) + modelo del road a Haiku.

## Pendiente
- Escalar el BTS al resto de las selecciones (decisión abierta).
- Footer hardcodea el número de sprint, quedó en 4d-3c; revisar para el público.

## Próximo (próxima sesión)
- **4d-4**: Giphy/Tenor para memes + hashtags activos del Mundial 2026.

## ADRs agregados en esta sesión
28, 29, 30. Más enmienda al 15 y aprendizaje en docs/CLAUDE.md.

## 4d-4a + fix journey
- footer simplificado a "Cábala · construido por Diego" (sin versión ni sprint).
- 4d-4a deployado: módulo "gifs del mundial" (giphy vivo por tribu, tira horizontal
  mp4 con carga por viewport, atribución texto) + botón "compartir cábala" en header
  (web share api + fallback clipboard). tenor descartado. adr 31 y 32.
- fix journey deployado: la cuota de youtube se agotaba por cache de 30 min; pasado a
  6h resultados / 10 min vacíos. adr 33. confirmado: volvió tras el reset de cuota.
- los gifs salieron de buena calidad; el riesgo de "gifs genéricos" no se dio.
- pendiente: 4d-4b (hashtags activos como queries del feed de bluesky).

## deuda técnica: logo giphy + cache en redis
- atribución de giphy: el asset oficial murió (dropbox borrado); se usó el logo svg de
  giphy + el texto "powered by". cumple los términos. ideal: el asset oficial si lo
  republican.
- cache persistente: vercel kv ya no existe (es upstash redis vía marketplace). helper
  lib/cache.ts. migrados journey, road y token de bluesky. gifs y enhancementCache
  quedan en memoria a propósito. adr 34.
- npm: el install de @upstash/redis dejó 3 vulnerabilidades transitivas; NO correr
  audit fix --force (rompe con cambios mayores).

## 4d-4b: hashtags + filtro del feed de bluesky
- hashtags reales como queries + búsquedas en paralelo. los tres traen volumen full
  (se desmintió la sospecha de poco volumen en bluesky).
- el volumen extra destapó que el filtro solo clasificaba; se sumó keep/reject estilo
  journey. rechaza política, activismo, crítica institucional a fifa, ruido. pool de 25.
  adr 35.
- incidente: al reemplazar enhanceWithClaude se borró sin querer la cabecera del GET
  (instrucción ambigua mía). se reinsertó. lección: anclar surgical con líneas
  concretas, no "hasta la llave de tal función".
- pendiente: cachear el feed en redis (carga en frío ~10s).

## cache del feed de bluesky
- el feed armado se cachea en redis con ttl 15min (helper lib/cache.ts, mismo patrón que
  journey/road). la carga en frío baja de ~10s a instantáneo y deja de reclasificar con
  haiku en cada visita. para forzar feed fresco ahora: ?refresh=true (reiniciar el server
  ya no alcanza, el feed vive en redis). aplica el patrón del adr 34, sin adr nuevo.

## sprint 5 — calendario real (football-data.org)
- API-Football descartado: su plan gratis no da la temporada 2026 (solo 2022-2024). lo
  cazó el autodiagnóstico del endpoint, ahora convención en CLAUDE.md.
- migrado a football-data.org (free tier incluye el mundial para siempre, ~10 req/min).
  endpoint /api/fixtures nuevo: /v4/competitions/WC/matches, filtra GROUP_STAGE, nombres
  EN→ES, hora argentina, cache redis 1h. adr 36.
- Calendar conectado al endpoint (antes hardcodeado). 72 partidos de grupos reales.
- football-data no da estadio; se muestra el grupo (A-L) en su lugar.
- pendiente sprint 5: GroupStage (posiciones), resultados, bracket.

## sprint 5 — grupos reales (GroupStage)
- las 12 tablas se derivan de los partidos (no del endpoint /standings, que da 404 para
  competiciones tipo cup). equipos del sorteo + puntos calculados de los finalizados. endpoint
  /api/standings, cache 1h, autodiagnóstico. adr 37.
- mapa de nombres EN→ES extraído a lib/teams.ts (lo usa standings; fixtures sigue con su copia,
  deuda menor de unificar).
- GroupStage conectado: 48 equipos reales, PJ/Pts (cero hoy). desempate aproximado, no el FIFA.
- pendiente sprint 5: resultados en vivo, bracket.

## corazón v1 — el pulso global real (wikipedia)
- norte definido: cábala = el pulso emocional del mundo (visión de diego, el dashboard de la
  pandemia dado vuelta a lo positivo). adr 38.
- el pulso/sentimiento/intensidad del header y mapa era todo Math.random (fake).
- pulso global real: endpoint /api/pulse, atención mundial vía wikipedia (6 idiomas del
  artículo del mundial), 0-100 + tendencia semanal + serie 16d, cache 6h. header con número
  real + ↑%. al hacerlo: pulse 82, +27% semanal, serie claramente ascendente.
- google trends descartado (sin api oficial). bluesky (la voz) como segunda capa futura.
- pendiente: sentimiento por selección + círculos del mapa siguen fake → reales en el próximo paso.

## robustez fixtures/standings (stale-on-error + reintento)
- bug: calendario y grupos vacíos en prod, ok en local, redeploy los "arreglaba".
- causa real (confirmada en código): NO era prerender en build. era el cache de 1h expirando +
  football-data con un hipo en frío + front sin reintento → pegado. el redeploy era un rodeo.
- fix: endpoints sirven el último bueno (stale-on-error, cache 7d + maxAge 1h, res.ok, nunca
  cachear vacío); front reintenta 4x con backoff. ADR 39. convención nueva en CLAUDE.md.
- anotado: road tarda ~15s en frío por Haiku (cacheado después).
